<!DOCTYPE html>
<html>
<head>
  <title>Client List</title>
</head>
<body>
  <h1>顧客一覧</h1>
  <div>
    <% @clients.each do |client| %>
      <div>
        <%= link_to "#{client.client_lastname} #{client.client_firstname}", client_path(client) %>
      </div>
    <% end %>
  </div>
</body>
</html>